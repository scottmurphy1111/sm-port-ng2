import { VertNavItemDirective } from './vert-nav-item.directive';

describe('VertNavItemDirective', () => {
  // it('should create an instance', () => {
  //   const directive = new VertNavItemDirective();
  //   expect(directive).toBeTruthy();
  // });
});
